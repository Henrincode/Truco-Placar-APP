import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        gap: 4,
        padding: 10,
        borderRadius: 10,
        backgroundColor: '#222'
    },
    texto: {
        color: 'white',
        fontSize: 24,
        textAlign: 'center'
    }
})

export default styles